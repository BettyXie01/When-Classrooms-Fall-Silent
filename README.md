# When Classrooms Fall Silent

A static webpage for a convergent journalism project about the closure of private kindergartens in China.

## Project Structure

- `index.html` is the page entry point for GitHub Pages.
- `image/` contains all image assets used by the page.

## Deployment

This project can be hosted with GitHub Pages by publishing the repository from the main branch root.
